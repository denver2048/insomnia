# kube-prometheus-stack overrides for Insomnia (via Flux HelmRelease).
# Scrape metrics every 10 seconds for faster alerting/dashboards.
prometheus:
  prometheusSpec:
    scrapeInterval: 10s
    evaluationInterval: 10s
